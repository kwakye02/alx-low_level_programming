#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
*main - runs main code
*Return: returns 0
*/

int main(void)
{
	char A;

	for (A = 'a'; A <= 'z'; A++)
		putchar(A);
	putchar('\n');

	return (0);
}
