#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
*main - runs main code
*Return: returns 0
*/

int main(void)
{
	for (char A = 'a'; A <= 'Z'; A++)
		putchar(A);
	putchar(\n);

	return (0);
}
