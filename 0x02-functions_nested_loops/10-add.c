#include "main.h"

/**
 * add - adds two integers and returns the result
 * @i: integer  x
 * @x: integer i
 * Return: returns the sum of i and x
 */
int add(int i, int x)
{
	int a;

	a = i + x;

	return (a);
}
